defmodule Cizen.Dispatcher.NodeTest do
  use ExUnit.Case

  describe "push" do
    test "pushes an event to next nodes"
    test "sends subscribers and next nodes to sender before pushing an event to next nodes"
  end

  describe "put" do
    # 1
    test "put true"
    # x -> x + 1 (mock)
    test "put operation"
    test "put :=="
    test "put not"
    test "put and"
    test "put or"
  end

  describe "delete" do
    # 1
    test "delete true"
    # x -> x + 1 (mock)
    test "delete operation"
  end
end
