defmodule Cizen.Dispatcher.IntakeTest do
  use ExUnit.Case

  test "starts sender with nil for first time of starting"
  test "starts sender with preceding one for second or later"
  test "pushes an event to root node"
  test "send root node to sender before pushing an event to root node"
end
