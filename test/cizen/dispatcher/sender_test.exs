defmodule Cizen.Dispatcher.SenderTest do
  use ExUnit.Case

  test "sender sends event to subscribers"
  test "sender does not sends event to subscribers if preceding sender is alive"
  test "sender exits"
  test "sender does not exit event to subscribers if event is not given"
  test "sender does not exit event to subscribers if waiting one or more nodes"
  test "sender does not exit event to subscribers if preceding sender is alive"
end
